// UDS Laporan placeholder — Phase 1 will render UDS reports
import { PageShell } from "@/components/page-shell";

export default function UdsLaporanPage() {
  return (
    <PageShell>
      <h1 className="text-2xl font-semibold mb-6">Laporan UDS</h1>
      <p className="text-muted-foreground">
        Laporan UDS (harian, bulanan, tahunan) akan dipaparkan di sini.
      </p>
    </PageShell>
  );
}